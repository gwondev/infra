name: 🚀 Deploy Infra (Direct Git Pull)

on:
  push:
    branches:
      - main  # main 브랜치 푸시 시 실행

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🌐 서버 접속 및 배포 (Git Pull + Compose)
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_KEY }}
          port: 22
          script: |
            set -e
            echo "=== 📂 Infra 배포 디렉토리 이동 ==="
            cd ~

            # 기존 infra 폴더 있으면 강제 동기화
            if [ -d "infra/.git" ]; then
              echo "✅ 기존 infra 폴더 발견 — 최신 코드 강제 동기화"
              cd infra
              git fetch --all
              git reset --hard origin/main
              git clean -fdx
            else
              echo "🆕 infra 폴더 없음 — 새로 클론"
              git clone https://github.com/${{ github.repository }} infra
              cd infra
            fi

            echo ""
            echo "=== 🧱 Docker Compose 빌드 ==="
            docker compose build --no-cache

            echo ""
            echo "=== 🚀 컨테이너 재시작 중... ==="
            docker compose down --remove-orphans
            docker compose up -d

            echo ""
            echo "=== ✅ Infra 서비스 재배포 완료 ==="
            docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}"
