name: 🚀 Deploy Infra (Local Build)

on:
  push:
    branches:
      - main  # main 브랜치 푸시 시 실행

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 리포지토리 가져오기
      - name: 📦 리포지토리 가져오기
        uses: actions/checkout@v4

      # 2️⃣ 서버에 SSH 접속하여 직접 빌드 및 실행
      - name: 🌐 서버 접속 및 배포
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_KEY }}
          port: 22
          script: |
            set -e
            echo "=== 📂 Infra 배포 디렉토리 이동 ==="
            cd ~

            # 폴더 존재 여부 확인 후 처리
            if [ -d "infra/.git" ]; then
              echo "✅ 기존 infra 폴더 발견 — 최신 코드 갱신"
              cd infra && git pull
            else
              echo "🆕 infra 폴더 없음 — 새로 클론"
              git clone https://github.com/${{ github.repository }} infra
              cd infra
            fi

            echo ""
            echo "=== 🧱 Docker Compose 빌드 ==="
            docker compose build

            echo ""
            echo "=== 🚀 컨테이너 재시작 중... ==="
            docker compose up -d --remove-orphans

            echo ""
            echo "=== ✅ Infra 서비스 재배포 완료 ==="
            docker ps --format "table {{.Names}}\t{{.Image}}\t{{.Status}}"
